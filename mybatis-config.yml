server:
  port: 21220
  
spring:
  profiles:
    active: dev
    
---

spring:
  profile: dev
  application:
    name: mybatis-service
  rabbitmq:
    host: localhost
    port: 5672 
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:21211/eureka/
    registry-fetch-interval-seconds: 3
    
management:
  security:
    enabled: false
    
debug: true

logging:
  file: mybatis.log
